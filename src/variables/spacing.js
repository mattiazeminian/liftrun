// src/variables/spacing.js

/**
 * Valori di spacing (spaziature) usati nell'app
 * Uniforma padding, margin e gap tra elementi per mantenere coerenza visiva.
 */

const Spacing = {
    none: 0, 
    xxs: 2,        // nessuno spazio, utile per override
    xs: 4,           // spaziatura extra piccola
    sm: 8,           // spaziatura piccola
    m: 12,
    md: 16,          // spaziatura media (di base)
    lg: 24,          // spaziatura grande
    xl: 32,          // spaziatura extra grande
    xxl: 48,         // spaziatura molto grande, titoli o grandi padding
};

export default Spacing;
